"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var imageDprUrl = function (asset, options) {
    var dpi = typeof window === 'undefined' || !window.devicePixelRatio
        ? 1
        : Math.round(window.devicePixelRatio);
    var imgH = (options === null || options === void 0 ? void 0 : options.height) ? (options === null || options === void 0 ? void 0 : options.height) * Math.max(1, dpi) : undefined;
    var imgW = options.width * Math.max(1, dpi);
    var urlParams = new URLSearchParams();
    urlParams.append('fit', 'max');
    urlParams.append('w', imgW.toString());
    if (imgH) {
        urlParams.append('h', imgH.toString());
    }
    return asset.url + "?" + urlParams.toString();
};
exports.default = imageDprUrl;
//# sourceMappingURL=imageDprUrl.js.map