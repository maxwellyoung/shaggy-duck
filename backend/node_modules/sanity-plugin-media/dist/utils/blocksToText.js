"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var defaults = { nonTextBehavior: 'remove' };
// Serialize portable text to plain text
// Based off: https://www.sanity.io/docs/presenting-block-text#plain-text-serialization
function default_1(blocks, opts) {
    if (blocks === void 0) { blocks = ''; }
    if (opts === void 0) { opts = {}; }
    if (typeof blocks === 'string') {
        return blocks;
    }
    if (!Array.isArray(blocks)) {
        return '';
    }
    var options = Object.assign({}, defaults, opts);
    return blocks
        .map(function (block) {
        if (block._type !== 'block' || !block.children) {
            return options.nonTextBehavior === 'remove' ? '' : "[" + block._type + " block]";
        }
        return block.children.map(function (child) { return child.text; }).join('');
    })
        .join('\n\n');
}
exports.default = default_1;
//# sourceMappingURL=blocksToText.js.map