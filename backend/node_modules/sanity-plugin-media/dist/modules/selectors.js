"use strict";
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spread = (this && this.__spread) || function () {
    for (var ar = [], i = 0; i < arguments.length; i++) ar = ar.concat(__read(arguments[i]));
    return ar;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.selectCombinedItems = void 0;
var toolkit_1 = require("@reduxjs/toolkit");
exports.selectCombinedItems = toolkit_1.createSelector([
    function (state) { return state.assets.allIds; },
    function (state) { return state.uploads.allIds; }
], function (assetIds, uploadIds) {
    var assetItems = assetIds.map(function (id) { return ({ id: id, type: 'asset' }); });
    var uploadItems = uploadIds.map(function (id) { return ({ id: id, type: 'upload' }); });
    var combinedItems = __spread(uploadItems, assetItems);
    return combinedItems;
});
//# sourceMappingURL=selectors.js.map